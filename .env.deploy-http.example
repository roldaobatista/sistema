# =============================================================================
# Variáveis para deploy via IP (HTTP, sem domínio)
# Copie para .env na raiz: cp .env.deploy-http.example .env
# Servidor: 178.156.176.145
# =============================================================================

# --- Frontend (Vite build args) ---
# Deixe vazio para usar mesma origem (IP ou domínio) — funciona com ambos
VITE_API_URL=
VITE_REVERB_HOST=
VITE_REVERB_PORT=
VITE_REVERB_SCHEME=

# --- Reverb (WebSocket) ---
REVERB_APP_KEY=kalibrium-key

# --- Banco de dados (ALTERE as senhas!) ---
DB_ROOT_PASSWORD=ALTERE_ESTA_SENHA
DB_DATABASE=kalibrium
DB_USERNAME=kalibrium
DB_PASSWORD=ALTERE_ESTA_SENHA
